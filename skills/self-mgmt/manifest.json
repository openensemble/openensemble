{
  "id": "self-mgmt",
  "name": "Self Management",
  "always_on": true,
  "hidden": true,
  "systemPromptAddition": "## Role management\n\nWhen {{USER_NAME}} asks you to take on, manage, or delegate yourself to a role (e.g. \"handle coding\", \"you're my coder now\", \"delegate yourself as my coder\"), use the claim_role tool with the appropriate role ID to formally claim it. This gives you the tools for that role. Don't just say you'll do it — actually call claim_role.\n\nOnly call claim_role when {{USER_NAME}} explicitly asks for the change. Never call it to answer questions about your role, capabilities, or what you do — answer those from your system prompt instead.\n\n## Standing instructions (per-skill rules)\n\nWhen {{USER_NAME}} says \"always do X with <skill>\" or gives a standing instruction for ANY skill you hold — built-in roles (gmail, gcal, expenses) or custom skills you own (youtube-downloader, pokemon-etb, anything created via skill-builder) — call `skill_add_rule({skillId, rule})`. The rule is appended to your per-user rules file for that skill and injected into your system prompt every turn you hold the skill, so it survives across conversations.\n\n**Strict ownership.** You may ONLY write rules for skills YOU currently hold. If {{USER_NAME}} asks you to add a rule to a skill held by a different agent, you MUST delegate via ask_agent to the holding agent — even if you're the coordinator. The tool will reject ownership mismatches with a denial message naming the current owner; honour that, don't try to work around it. Each agent is the sole authority over its own skills.\n\nUse `skill_list_rules({skillId})` to see existing rules with indices, then `skill_remove_rule({skillId, index})` to drop one. List works for any skill (no ownership check); mutation is owner-only.\n\n## Durable memory\n\n**Hard rule — no hallucinated memory.** If {{USER_NAME}} asks you to remember, save, note, record, preserve, or \"keep track of\" information — whether a single fact or the findings of a dive, investigation, or inventory — you MUST call `remember_fact` for each durable item BEFORE telling {{USER_NAME}} it's saved. Never respond with \"I'll remember that\", \"noted\", \"saved\", \"got it\", \"I'll keep that in mind\", or any equivalent without first calling the tool. If you did a multi-step investigation (e.g. listed nodes, probed each one), call `remember_fact` once per distinct durable finding — not a single summary. Claiming you remembered without the tool call is a bug; {{USER_NAME}} will lose the information on the next conversation and it will be your fault.\n\nFacts you learn about {{USER_NAME}}'s environment, infrastructure, people, or standing decisions should be preserved across conversations so no agent has to re-discover them. Use these tools:\n\n- **remember_fact** — Call AFTER you've confirmed something durable (typically via tool use — probing a node, checking a file, reading config) AND whenever {{USER_NAME}} asks you to remember/save/note something. Good fact: 'host X is a Proxmox VE 8.2 server at 10.0.0.10, 32GB RAM'. Bad fact: 'it's a server' (too vague), 'the weather is nice' (ephemeral). Facts are auto-scoped to your current service role by default — infra facts you learn while holding the nodes role only get injected into nodes-role agents' contexts, keeping other agents' prompts clean. Pass `scope: 'shared'` only for universal facts about {{USER_NAME}} (name, location, timezone, cross-cutting preferences).\n\n- **recall_facts** — Call BEFORE asking {{USER_NAME}} something that might already be known (server IPs, people's roles, past decisions). This saves the user from re-explaining context.\n\n- **forget_fact** — When {{USER_NAME}} corrects a stored fact or says it's no longer true.\n\n{{USER_NAME}} saying \"remember X\" or \"this is a fact: X\" is auto-pinned by the system before you see it — you only need to confirm briefly. Call remember_fact yourself when you discover something worth preserving through your own tool use, or when {{USER_NAME}}'s request to \"remember\" is more complex than a single literal fact (e.g. \"dive into X and remember what you find\").",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "skill_add_rule",
        "description": "Add a persistent rule to a skill you own. Works for built-in roles (gmail, gcal, expenses, etc.) AND custom skills (youtube-downloader, pokemon-etb-preorders, anything created via skill-builder). The rule is appended to users/<you>/role-rules/<skillId>.md and injected into the system prompt of whichever agent holds the skill from the next conversation. STRICT OWNERSHIP: only the agent currently holding a skill may write its rules — even the coordinator can't write rules for a specialist's skill. If a non-owner tries, the tool returns a denial message telling them to ask_agent the holding agent. Use skill_list_rules first to confirm the skill id; use skill_remove_rule with the index to drop a rule.",
        "parameters": {
          "type": "object",
          "properties": {
            "skillId": {
              "type": "string",
              "description": "ID of the skill to add the rule to (e.g. 'gmail', 'youtube-downloader', 'pokemon-etb-preorders')."
            },
            "rule": {
              "type": "string",
              "description": "The rule to add, written as a clear instruction (e.g. 'Always use HTML email with clickable thumbnail when notifying about new YouTube uploads')"
            }
          },
          "required": [
            "skillId",
            "rule"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_remove_rule",
        "description": "Remove a previously added rule from a skill you own. Use skill_list_rules first to see the current rules and their indices. Strict ownership applies — only the holding agent may remove rules.",
        "parameters": {
          "type": "object",
          "properties": {
            "skillId": {
              "type": "string",
              "description": "ID of the skill"
            },
            "index": {
              "type": "integer",
              "description": "Zero-based index of the rule to remove, as shown by skill_list_rules"
            }
          },
          "required": [
            "skillId",
            "index"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "set_email_send_without_confirm",
        "description": "Toggle the per-user email send-without-confirmation gate. When ON, email agents (Gmail, Microsoft, IMAP) send replies and new emails directly when the user explicitly asks — no draft preview, no 'are you sure?'. When OFF (default), email agents always show a draft and wait for explicit approval. Call this when the user says things like 'stop asking me to confirm emails', 'just send emails directly', 'turn off email confirmation', or the inverse ('require confirmation before sending email', 'always ask before sending'). The setting persists across conversations until the user changes it again. Note: this only affects sends the user explicitly asked for in their message; drafts the agent proactively suggested still wait for approval regardless.",
        "parameters": {
          "type": "object",
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "true to turn OFF the confirmation gate (send directly when user asks); false to require draft + confirm before every send"
            }
          },
          "required": [
            "enabled"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_list_rules",
        "description": "List all user-added rules for a skill (built-in role or custom skill). Anyone can call this — listing isn't gated by ownership, only mutation is. Shows each rule with its index so rules can be removed via skill_remove_rule.",
        "parameters": {
          "type": "object",
          "properties": {
            "skillId": {
              "type": "string",
              "description": "ID of the skill to list rules for"
            }
          },
          "required": [
            "skillId"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "claim_role",
        "description": "Claim or release a role so that you — and only you — handle it. ONLY call this when the user explicitly asks you to take over or manage a specific role (e.g. 'I want you to handle my calendar'). Never call this to answer questions about your role, capabilities, or what you do — answer those from your system prompt instead. Before claiming a role someone else owns, tell the user who currently has it and ask for confirmation, then call again with force=true. System/delegate roles cannot be assigned.",
        "parameters": {
          "type": "object",
          "properties": {
            "roleId": {
              "type": "string",
              "description": "ID of the role to claim or release (e.g. 'gcal', 'tasks', 'web')"
            },
            "force": {
              "type": "boolean",
              "description": "Set true to claim even if currently owned by another agent — only after the user has confirmed the transfer"
            },
            "release": {
              "type": "boolean",
              "description": "Set true to release your ownership of the role (returns it to all agents)"
            }
          },
          "required": [
            "roleId"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "remember_fact",
        "description": "Pin a durable fact to memory. Facts are immortal (never decay) and injected into the system prompt on every future turn. By default, facts learned while you hold a service role (nodes, email, gcal, etc.) are auto-scoped to that role — only agents holding that role will see them. This keeps each agent's context clean: e.g. infra facts stay with the nodes-role holder and don't clutter the email agent's prompt. Use when you learn something load-bearing via tool use — probing a node, reading config, confirming a policy. Do NOT call for ephemeral details, chit-chat, or casual preferences. The user saying 'remember X' or 'this is a fact: X' is already auto-pinned by the system.",
        "parameters": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "The fact in one clear sentence, framed so it makes sense out of context. Include specifics (names, IPs, versions, paths). Good: 'host X is a Proxmox VE 8.2 server at 10.0.0.10, 32GB RAM, runs 8 VMs'. Bad: 'it's a server'."
            },
            "scope": {
              "type": "string",
              "description": "Optional override. Omit for auto-scoping (recommended — scopes to your current service role if you hold exactly one). Pass 'shared' to force the fact to be visible to every agent regardless of role (use for universal truths about the user: name, timezone, location, cross-cutting preferences). Pass a role id (e.g. 'nodes', 'email') to force that scope."
            }
          },
          "required": [
            "text"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "recall_facts",
        "description": "Search stored facts and past conversation episodes for anything relevant to a query. Use this BEFORE asking the user something that may already be known (e.g. server IPs, people's roles, past decisions). Returns up to 10 matches ranked by relevance.",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "What to search for — e.g. 'proxmox server', 'my wife's name', 'backup schedule'"
            }
          },
          "required": [
            "query"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "forget_fact",
        "description": "Remove a fact or memory by describing it. Semantic search finds the best-matching memories and soft-deletes them. Use when the user asks to forget something or when you detect a stored fact has become wrong. Immortal pins are deletable via this path too — unlike normal forget, which refuses immortals.",
        "parameters": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "Describe the memory to forget, e.g. 'my old Proxmox host' or 'the old backup schedule'"
            }
          },
          "required": [
            "text"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "teach_fastpath_phrase",
        "description": "Teach a user-specific trigger phrase for a skill's local fast-path: after this, saying the phrase runs the skill's tool instantly on-device with NO cloud-LLM call. Call when the user says things like 'when I say X, run/check Y', 'make \"blitz it\" run the weather skill', 'add a shortcut phrase for my publix skill'. The phrase is stored per-user and takes effect on the next message. If the skill has multiple tools and it's ambiguous which one the phrase should run, pass `tool`.",
        "parameters": {
          "type": "object",
          "properties": {
            "skill_id": {
              "type": "string",
              "description": "The skill's id (or exact name) that the phrase should trigger"
            },
            "phrase": {
              "type": "string",
              "description": "The exact phrase the user wants to say (3-200 chars)"
            },
            "tool": {
              "type": "string",
              "description": "Which of the skill's tools the phrase runs — required only when the skill has several tools and no clear default"
            }
          },
          "required": [
            "skill_id",
            "phrase"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "forget_fastpath_phrase",
        "description": "Remove a previously taught fast-path trigger phrase from a skill, so the phrase routes through the normal assistant flow again. Call when the user says 'forget/remove that phrase', 'stop running the weather skill when I say X'.",
        "parameters": {
          "type": "object",
          "properties": {
            "skill_id": {
              "type": "string",
              "description": "The skill the phrase was taught for"
            },
            "phrase": {
              "type": "string",
              "description": "The phrase to forget (as originally taught)"
            }
          },
          "required": [
            "skill_id",
            "phrase"
          ]
        }
      }
    }
  ]
}