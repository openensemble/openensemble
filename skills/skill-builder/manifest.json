{
  "id": "skill-builder",
  "name": "Skill Builder",
  "description": "Create, update, and delete custom skills on demand",
  "icon": "🔩",
  "category": "service",
  "always_on": false,
  "enabled_by_default": false,
  "alias_catalog": {
    "entity_kind":   "skill",
    "noun_singular": "skill",
    "noun_plural":   "skills",
    "extra_phrase_patterns": [
      "\\bskill\\s+(?:called\\s+|named\\s+)?([A-Za-z][A-Za-z0-9 _-]{1,40})\\b"
    ],
    "catalog_source": {
      "type": "exported_function",
      "function": "listAliasEntries"
    },
    "id_field":     "id",
    "name_fields":  ["name"],
    "id_arg_names": ["id", "skill_id", "skillId"],
    "cascade_on_tools": [
      { "tool": "skill_delete", "id_arg": "id" }
    ]
  },
  "systemPromptAddition": "You can create, update, and delete custom skills using the skill-builder tools. IMPORTANT: Before calling skill_create or skill_update_code, you MUST first call skill_read_blueprint to read the skill authoring guide. Never skip this step. EVERY skill_create call MUST include `intent_examples` — 6-15 short natural-language phrases the user would say when wanting this skill. The classifier embeds these at boot and uses them to decide when to load the skill's tools without forcing an LLM round-trip. Skip examples and the skill is only reachable when the LLM explicitly asks for it via request_tools — fine for niche skills, wasteful for common ones. Also pick a `coordinator_scope`: 'include' (default — always available to the coordinator), 'auto' (only loaded when intent matches — preferred for skills useful sometimes), or 'exclude' (specialist-only, e.g. GPU-pod managers or finance ingestion that belong on a specialist agent and would pollute the coordinator with 10+ KB of tools on every turn). The blueprint covers the full skill API including: tool definitions, ctx primitives (showImage/showVideo/watch/unwatch/proposeMonitor), watchers (background polling for long-running work — video gen, training, price alerts, pod lifecycle), drawers (optional sidebar UI), config storage, and importing app internals. When the user describes a skill that involves long-running async work (anything taking more than a few seconds, or anything that needs to monitor an external state like a queue/job/price), you MUST scaffold it with a watcher — declare `manifest.watchers: [...]`, register via `ctx.watch(...)` from the kickoff tool, and define `watcherHandlers` in execute.mjs. Estimate `expiresAt` from realistic worst-case work duration; use `expiresAt: null` for genuinely-indefinite watchers (price alerts, condition triggers).\n\n### MONITORED-SOURCE PATTERN (proactive skills)\nWhen the user says any variant of `tell me when new X arrives`, `ping me when X changes`, `make a tracker for X`, `monitor X for me`, `check X every week`, `let me know when there are new BOGOs / uploads / deals / posts / releases` — the skill MUST be proactive. Use this four-piece scaffold every time, in order:\n\n1) **Fetcher** — a tool that hits the source and returns the current state. Choose the leanest possible source (RSS > public API > JSON-LD on the page > full scrape).\n2) **Cadence** — pick a preset on `ctx.proposeMonitor`: `minutely` | `fast` (5 min) | `hourly` | `daily` | `weekly`. Smart defaults: store ads → `weekly`; channel/feed → `hourly`; price → `fast`; release notes → `daily`. If the source has a known refresh day (Publix ads rotate Wednesdays), tick on `daily` and no-op in the handler until it's Wednesday.\n3) **Pref-aware filter** — read user prefs from cortex before notifying: `const { searchMemory } = await import('../../memory.mjs'); const hits = await searchMemory(userId, '<topic> preferences', 8);` and only emit a textUpdate for items that match. Without this, every poll spams the user with unfiltered changes.\n4) **Delivery** — register via `ctx.proposeMonitor({ kind, state, cadence, label, expiresAt: null, deliver, skillId: '<your-skill-id>' })`. Pick `deliver` BASED ON THE USER'S EXPLICIT INTENT:\n   - `'agent'` (default) — runs a turn so the LLM can summarize + TTS naturally. Pair with `agentPrompt: '...'`. Use unless the user asks otherwise.\n   - `'email'` — when the user says any variant of `email me`, `send it to my email`, `notify me by email`. Pair with `emailSubject: '...'`. Sends FROM the user's primary connected account TO their profile email — no agent turn, no `ask_agent` needed. NEVER route email delivery through `ask_agent` to an email agent; `deliver: 'email'` handles it directly via lib/email-delivery.mjs.\n   - `'telegram'` — when the user says `text me`, `send me a telegram`, `message me on telegram`, `ping me on telegram`. Optionally pair with `telegramPrefix: '...'` for a header line. Sends via the user's linked Telegram bot — no agent turn, no `ask_agent` needed. NEVER route Telegram delivery through `ask_agent`; `deliver: 'telegram'` handles it directly via routes/telegram.mjs:sendTelegramToUser.\n   - `'notify'` — terse status bubble only. Reserve for cases where the user explicitly said 'just a notification, don't tell me about it' or for very low-importance ambient signals.\n   In the handler, call `helpers.fire(message)` instead of `helpers.fireAgent` — `fire` dispatches based on whatever `deliver` was chosen, so the same handler emails OR speaks depending on registration. For `'email'`, write `message` as a plain-prose email body (no TTS framing). For `'agent'`, write it as an instruction to the LLM.\n\nWhen the user changes their mind ('actually email me instead'), the kickoff tool should support a `deliver` parameter and, if a watcher already exists for the same `dedupKey` with a DIFFERENT delivery, drop it via `ctx.unwatchMatching` before re-registering. Otherwise the dedup check silently keeps the old delivery mode.\n\nIdempotency: pass `dedupKey: '<stable-identity>'` (channel id, sender email, store id) so calling the kickoff tool twice doesn't stack N watchers for the same source.\n\n**CRITICAL — `skillId` field MUST equal the manifest `id` VERBATIM.** The `usr_` prefix is a legacy format; the modern migration drops it. If your manifest declares `\"id\": \"my-skill\"`, then `skillId: 'my-skill'` — NOT `'usr_my-skill'`. Getting this wrong means the watcher supervisor can't find your `watcherHandlers` export and the watcher logs \"Handler not found\" every tick until it fails out. Always declare a single `const SKILL_ID = '<exact-manifest-id>';` at the top of execute.mjs and reuse it everywhere proposeMonitor is called.\n\nExample skeleton for `kind: 'mysite_check'`:\n```\nctx.proposeMonitor({\n  kind: 'mysite_check',\n  state: { url: 'https://...', lastSeen: null },\n  cadence: 'weekly',\n  label: 'Watch <site> for new posts',\n  expiresAt: null,\n  deliver: 'agent',                              // or 'email' / 'notify'\n  agentPrompt: 'Summarize the new <site> posts in one or two spoken sentences.',\n  // emailSubject: 'New posts on <site>',         // when deliver='email'\n  skillId: 'mysite',\n  dedupKey: 'mysite-default',\n});\n// watcherHandlers.mysite_check returns { textUpdate, done, newState }; call\n// `await helpers.fire(message)` to deliver via whatever mode was chosen.\n```\n\n### USER-NAMED-CATALOG PATTERN (alias_catalog)\nWhen a skill maintains a list of items the user references by name — watched channels, saved searches, tracked addresses, accounts, subscriptions, recipes — declare an `alias_catalog` block in the manifest. The skill-alias framework (lib/skill-alias-framework.mjs) then runs a per-turn resolver that recognizes phrases like `the <X> <noun>` and prepends a system note with the resolved id, so the LLM skips the `list_X` → match-by-name dance.\n\nWhen to add this: ANY tool that takes an opaque id (channel_id, account_id, item_id, …) AND the user typically refers to that thing by a friendly name. If the skill enumerates and then filters on every call, add `alias_catalog`.\n\nSchema (drop the whole block into manifest.json next to `tools`):\n```\n\"alias_catalog\": {\n  \"entity_kind\":   \"yt_channel\",          // globally unique across all skills\n  \"noun_singular\": \"channel\",             // matcher generates \"the X <noun>\"\n  \"noun_plural\":   \"channels\",\n  \"extra_phrase_patterns\": [               // OPTIONAL — domain-specific regexes\n    \"\\\\bnew\\\\s+(?:videos?|uploads?)\\\\s+from\\\\s+(.+?)\\\\b\"\n  ],\n  \"catalog_source\": {                      // ONE of two modes:\n    \"type\":      \"config_file\",\n    \"path\":      \"users/{userId}/<your-config>.json\",\n    \"json_path\": \"watchedChannels\",        // dot-path; optional\n    \"format\":    \"object_map\"               // \"object_map\" | \"array\"\n  },\n  // OR for non-file storage (cortex, encrypted, remote):\n  // \"catalog_source\": { \"type\": \"exported_function\", \"function\": \"listAliasEntries\" }\n\n  \"id_field\":      \"channelId\",            // path within an entry\n  \"name_fields\":   [\"channelName\"],        // ordered fallback list\n  \"id_arg_names\":  [\"channel_id\", \"channelId\"],   // learner hooks on these arg names\n  \"cascade_on_tools\": [                     // alias auto-dropped when tool succeeds\n    { \"tool\": \"<your-skill>_unwatch\", \"id_arg\": \"channel_id\" }\n  ]\n}\n```\n\nWhat the framework gives you for free:\n- Per-turn phrase resolution against the user's text — no skill code needed.\n- Learner that observes any tool call with a matching id_arg_name and saves the user's phrase → id mapping (so the second mention pre-resolves).\n- Catalog cache with 5-min TTL, auto-invalidated when a cascade tool fires.\n- Cascade-delete on stored aliases when an entry is removed.\n- Clarification path: if the LLM asks `did you mean <X>?` and the user replies `yes`, the framework saves the alias.\n\nNo new code in execute.mjs — purely declarative. If your catalog isn't readable via a file path (encrypted with ctx.getCredential, fetched from a remote API, stored in cortex memory), use `\"type\": \"exported_function\"` and export `listAliasEntries(userId) → Promise<Array<{...}>>` from execute.mjs returning the catalog entries.\n\nWhen a skill writes files to disk (downloads, generated media, exported documents), you MUST save them under the user's profile via `getUserFilesDir(userId, kind)` from `lib/paths.mjs` — pick `kind` from `videos | images | documents | research | code` based on what's being saved. NEVER write to `~/Downloads`, `os.homedir()`, or any path outside the install dir; outputs land in the user's Profile Files UI for free this way.\n\n**CRITICAL — credentials NEVER touch a plaintext config file.** If the skill needs an API key, password, OAuth token, or any other secret from the user, use the credential helpers on the executor's `ctx` parameter (5th arg): `await ctx.getCredential(id)` to read (decrypted), and `await ctx.requestCredential({id, label, kind:'api_key', persist:true})` if missing. NEVER `import` from `lib/credentials.mjs` directly — the relative-path depth differs between built-in and user skills and is easy to miscount. The chat widget collects the value over a protected WS frame, encrypts it with the user's master key, and stores under `users/<id>/credentials/<id>.json`. The plaintext value NEVER enters the LLM message history. Plaintext `usr_<skillId>-config.json` is fine for non-secrets (URLs, account IDs, preferences, timestamps) — but anything that would compromise the user if it leaked goes through the credential primitive. See the blueprint's `Storing configuration / secrets` section for the full executor signature. Skills may optionally include a drawer — a small UI panel that opens from the sidebar — but a drawer is entirely optional; most skills should not include one. Only add a drawer when the user explicitly asks for a visual panel. To delete a custom skill use skill_delete — never use shell commands or coder tools to remove skill directories. FIXING BUGS IN EXISTING SKILLS: when a bug is reported in a skill you already created, prefer skill_read_code + skill_patch_code over skill_update_code. skill_patch_code takes an array of targeted find/replace edits and avoids resending the entire file — use it for any change that fits as a focused edit. Only fall back to skill_update_code when the change is large or structural.",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "skill_read_blueprint",
        "description": "Read the skill authoring guide before writing any skill code. Always call this first.",
        "parameters": { "type": "object", "properties": {}, "required": [] }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_create",
        "description": "Create a new custom skill and load it immediately into the user's session without a server restart. The id is used verbatim — no automatic prefix. Tools will be available in the next message turn. Only use Node built-ins and packages in ~/.openensemble/node_modules.",
        "parameters": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Unique id: lowercase letters, numbers, hyphens (no underscores). Stored verbatim — use this exact string when later calling skill_delete, skill_read_code, or skill_patch_code." },
            "name": { "type": "string", "description": "Human-readable skill name" },
            "description": { "type": "string", "description": "One-sentence description" },
            "icon": { "type": "string", "description": "Single emoji icon" },
            "tools": {
              "type": "array",
              "description": "OpenAI-compatible tool schemas. Each: {type:'function', function:{name, description, parameters}}. Always prefix tool names with the skill id (e.g. ha_turn_on for skill id 'ha').",
              "items": { "type": "object" }
            },
            "watchers": {
              "type": "array",
              "description": "OPTIONAL. Declare the watcher kinds this skill registers. Each entry: { kind: 'string', description: 'string' }. Required when the skill kicks off long-running async work (video gen, training, price alerts, pod lifecycle, job queues). The kind strings must match keys in the watcherHandlers named export in code. Read the blueprint's 'Background work — watchers' section before using.",
              "items": {
                "type": "object",
                "properties": {
                  "kind":        { "type": "string", "description": "Unique watcher kind id, must match a key in watcherHandlers. Prefix with skill id like tool names." },
                  "description": { "type": "string", "description": "What this watcher polls / produces." }
                },
                "required": ["kind", "description"]
              }
            },
            "code": { "type": "string", "description": "Complete ESM source for execute.mjs. Must export: async function executeSkillTool(name, args, userId, agentId, ctx) and export default executeSkillTool. Return a string result. If the skill declares watchers, also export `watcherHandlers` — a named export object mapping each declared kind to an async handler function `(state, helpers) => ({textUpdate?, done?, newState?, nextCadenceSec?, extendExpiryBy?})`. Inside tool handlers, register watchers via `ctx.watch({kind, skillId, label, cadenceSec, expiresAt, state})` — always set expiresAt explicitly based on a realistic estimate of the work, or pass null for indefinite watchers (price alerts etc.). Never import app internals at the top level — use dynamic await import() inside handlers only." },
            "intent_examples": {
              "type": "array",
              "description": "STRONGLY RECOMMENDED. 6–15 short natural-language phrases the user might say when they want this skill. Used by the tool-router's embedding classifier to decide whether to load this skill's tools on a given turn — without examples, the coordinator only sees the skill's tools after the LLM explicitly calls request_tools. Write phrases the way real users phrase requests, varied across surface forms. Examples for a 'kroger-deals' skill: ['what's on sale at kroger this week', 'check kroger weekly ad', 'find snack deals at kroger', 'kroger digital coupons', 'are eggs on sale at kroger']. Don't include the skill name itself in the phrase — match how a user describes the GOAL, not the tool.",
              "items": { "type": "string" }
            },
            "coordinator_scope": {
              "type": "string",
              "enum": ["include", "exclude", "auto"],
              "description": "How this custom skill flows to the coordinator agent's tool surface. 'include' (default for custom skills): always available to the coordinator. 'exclude': hidden from the coordinator entirely — useful for heavyweight skills (GPU-pod managers, finance ingestion) that belong on a specialist agent and would otherwise add 10+ KB to every coordinator turn. 'auto': only loaded when the user prompt matches the skill's intent_examples (requires intent_examples to be set). Pick 'exclude' when the skill should only be reachable via ask_agent to a specialist. Pick 'auto' for skills useful sometimes but not every turn."
            },
            "drawer": {
              "type": "object",
              "description": "OPTIONAL. Provide ONLY if the user wants a sidebar drawer UI for this skill. Omit entirely otherwise — most skills should not include a drawer.",
              "properties": {
                "name":       { "type": "string", "description": "Drawer label shown in the header (e.g. 'Home Assistant')" },
                "lucideIcon": { "type": "string", "description": "STRONGLY PREFERRED. A lucide icon name (e.g. 'receipt', 'home', 'bell', 'calendar') rendered in the sidebar button and drawer header so the drawer matches the rest of the UI. See https://lucide.dev/icons for names. Fall back to emoji only if no lucide icon fits." },
                "icon":       { "type": "string", "description": "Single emoji, only used if lucideIcon is not provided. Prefer lucideIcon for visual consistency with built-in drawers." },
                "html":       { "type": "string", "description": "HTML injected into the drawer body. Use plain HTML and the .cdraw-* classes documented in the skill blueprint (.cdraw-row, .cdraw-btn, .cdraw-badge, .cdraw-empty, .cdraw-toolbar, etc.) so the drawer matches the app's look and feel. Give any custom ids a unique prefix (e.g. usr_myskill_root) to avoid DOM collisions." },
                "initJs":     { "type": "string", "description": "OPTIONAL JS body that runs when the drawer is opened. Runs in the page global scope; use `document.getElementById(...)` and `fetch('/api/usr_myskill/...')` freely." },
                "serverCode": { "type": "string", "description": "OPTIONAL ESM source for plugins/usr_{id}/server.mjs. Must export: async function handleRequest(req, res, cfg). Return true if handled, false otherwise. Only needed if the drawer needs custom HTTP endpoints." }
              },
              "required": ["name", "html"]
            }
          },
          "required": ["id", "name", "description", "tools", "code"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_update_code",
        "description": "Replace the entire execute.mjs of an existing user-created skill and hot-reload it without a server restart. Prefer skill_patch_code for small fixes — only use this tool when the change is large or structural. Only the skill's creator or an admin/owner can update it.",
        "parameters": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Full skill id including usr_ prefix (e.g. usr_homeassistant)" },
            "code": { "type": "string", "description": "Complete new execute.mjs source code" }
          },
          "required": ["id", "code"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_read_code",
        "description": "Read the current execute.mjs source of a user-created skill. Use this before skill_patch_code so you know the exact strings to target for find/replace.",
        "parameters": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Full skill id including usr_ prefix" }
          },
          "required": ["id"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_patch_code",
        "description": "Apply one or more find/replace edits to a user-created skill's execute.mjs and hot-reload it, without resending the whole file. Each edit's 'find' string must appear EXACTLY ONCE in the current file — include surrounding context to make it unique. Edits are applied in order. If the patched file fails validation, the original is restored automatically. Use this for bug fixes and small changes instead of skill_update_code. Only the skill's creator or an admin/owner can patch it.",
        "parameters": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Full skill id including usr_ prefix" },
            "edits": {
              "type": "array",
              "description": "Edits to apply in order. Each must be an object with 'find' and 'replace' string fields.",
              "items": {
                "type": "object",
                "properties": {
                  "find":    { "type": "string", "description": "Exact substring to replace. Must appear exactly once in the current file — include enough surrounding context to make it unique." },
                  "replace": { "type": "string", "description": "Replacement string. May be empty to delete the matched region." }
                },
                "required": ["find", "replace"]
              },
              "minItems": 1
            }
          },
          "required": ["id", "edits"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_delete",
        "description": "Permanently delete a user-created skill and remove it from the session. Only the skill's creator or an admin/owner can delete it.",
        "parameters": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Full skill id including usr_ prefix" }
          },
          "required": ["id"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "skill_list",
        "description": "List all custom skills currently in the user's session.",
        "parameters": { "type": "object", "properties": {}, "required": [] }
      }
    }
  ]
}
